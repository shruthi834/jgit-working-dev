Collections.sort(draftMapsList, Collections.reverseOrder());
 @Override
    public int compareTo(DraftMap o) {
        if (getCreateDate() == null || o.getCreateDate() == null)
            return 0;
        return getCreateDate().compareTo(o.getCreateDate());

    }
