Collections.sort(draftMapsList, Collections.reverseOrder());
 @Override
    public int compareTo(DraftMap o) {
        if (getCreateDate() == null || o.getCreateDate() == null)
            return 0;
        return getCreateDate().compareTo(o.getCreateDate());

    }
// Assume you have a RevCommit object called 'commit' from which you want to get the timestamp
Date commitDate = commit.getWhen();

// Create a Calendar instance and set it to the commitDate
Calendar calendar = Calendar.getInstance();
calendar.setTime(commitDate);

// Add 8 hours to the Calendar
calendar.add(Calendar.HOUR, 8);

// Get the updated Date
Date updatedDate = calendar.getTime();
