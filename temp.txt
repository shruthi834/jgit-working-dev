List<String> codeListWithoutHeader = rows.subList(1, rows.size());
        for(String row:codeListWithoutHeader){
            String[] rowValueSplited=row.split(",");
            int id = jdbcTemplate.queryForObject("select IXMCM_TRANSLATION_CODE_SEQ.nextval from dual", Integer.class);
            codeListService.insertData(id,memberId,rowValueSplited[0],rowValueSplited[1],rowValueSplited[2],mapName,pledgeType);
        }
